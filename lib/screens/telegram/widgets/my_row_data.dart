import 'package:flutter/material.dart';

// Описывает, как обрабатывать данные с сервера
class MyRowData {
  IconData icon;
  Color color;
  String text;

  MyRowData({required this.icon, required this.color, required this.text});
}